---
"@lgrammel/agent-tui": patch
---

Show tool execution and next-step processing statuses while agent streams continue.

Allow configuring the context window size so the terminal UI can show token usage as a percentage.
